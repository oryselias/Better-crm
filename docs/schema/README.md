# Schema Notes

Keep durable data-model thinking here before and alongside SQL migrations.

Recommended files:
- `data-model.md` for entities and relationships
- `rls-strategy.md` for clinic tenancy and permissions
- `lab-report-ingestion.md` for raw PDF, parsed JSON, embeddings, and audit storage
- `audit-events.md` for append-only actor and row-level traceability
